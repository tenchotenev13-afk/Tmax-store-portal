# Локални dev зависимости (jsdom тестове) — не влизат в GitHub Pages deploy
node_modules/
npm-debug.log*

# Редакторски / OS файлове
.vscode/
.idea/
Thumbs.db
desktop.ini

# Claude Code — временни worktree-и (пълни копия на репото).
# .claude/skills/ НЕ се игнорира: skill-овете пътуват с репото нарочно.
.claude/worktrees/

# Контекстният пакет за чата се генерира при нужда (npm run brief),
# за да сочи печатът за версия точно текущия HEAD.
docs/BRIEF.md
