-- Rollback на 20260911080537_recurring_task_skips.sql
--
-- ⚠️ Първо се връща КЛИЕНТЪТ (и едж функциите, ако вече четат таблицата).
-- Изчезне ли таблицата, докато кодът още я чете, заявката връща PGRST205 —
-- sbGet показва червен toast и връща [], тоест всичко се брои както преди
-- изключванията, но с грешка при всяко отваряне на Бюлетина.
--
-- ⚠️ Трие всички записани изключвания и те НЕ се възстановяват. Преди това
-- виж колко са:   select count(*) from public.recurring_task_skips;
-- Изключените задачи отново излизат като дължими в съответните седмици,
-- включително в минали седмични отчети, ако бъдат пресметнати наново.
--
-- DROP TABLE маха политиките, индексите и FK-то. recurring_tasks не се пипа.

drop table if exists public.recurring_task_skips;
