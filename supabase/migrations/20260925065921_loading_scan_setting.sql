-- Приложено в Supabase на 25.09.2026, проект xiwkdiqqplgdcrkewgtv
-- Миграция: loading_scan_setting (version 20260925065921)
--
-- ДАННИ, не схема: нов ред в app_settings. НИЩО ЗА ЖИВКО.
--
-- Бутонът „📷 Сканирай" в блока „Артикули" (редакторът на склада и формата за
-- извънреден ред на обекта) се показва само при стойност 'on'. Тръгва
-- ИЗКЛЮЧЕН по същата причина като loading_transit_docs (23.09.2026): в
-- началото складът се обърква от твърде много начини да въведе един артикул.
-- SAP кодът и автодопълването остават — те са основният път.
--
-- Изключен, бутонът НЕ СЕ РЕНДИРА и html5-qrcode (375 KB, достъп до камера)
-- изобщо не се зарежда; llOpenScanner излиза веднага, ако бъде извикан отвън.
-- Скрит бутон, който пак дърпа библиотеката, не е скрит — той е невидим.
--
-- Клиентът (loading.js, llLoadFeatureFlags) приема САМО 'on'; всичко друго —
-- липсващ ред, празно, боклук, паднала заявка — значи изключено. Тоест дори
-- този insert да не мине, поведението е същото; редът е тук, за да има какво
-- да се смени. Двата ключа на модула се четат с ЕДНА заявка in.(…).
--
-- РЕГИСТЪРЪТ НА СКРИТОТО Е HIDDEN-FEATURES.md В КОРЕНА. Всяко ново скриване
-- се вписва там в същия комит — иначе следващият вижда липсващ бутон и тръгва
-- да „поправя" нещо, което е цяло.
--
-- Rollback: supabase/migrations/20260925065921_loading_scan_setting_down.sql

insert into public.app_settings (key, value, updated_by)
values ('loading_scan', 'off', 'claude 25.09.2026 — скенерът скрит по решение на Теодор')
on conflict (key) do nothing;
