-- Rollback на 20260912152519_postpone_date.sql
--
-- ⚠️ Първо се връща КЛИЕНТЪТ (и едж функциите, ако вече четат колоната).
-- Изчезне ли колоната, докато кодът я иска, PostgREST връща 42703 за всяка
-- заявка със postponed_to в select/филтъра: „Днес", Бюлетинът и отчетите
-- показват червен toast и празни списъци.
--
-- ⚠️ Трие ДАТИТЕ на всички отлагания — те НЕ се възстановяват. Самите
-- отложени редове остават (status='postponed', причината и първоначалният
-- срок си стоят), тоест поведението се връща към „отложена, без за кога":
-- задачата не се връща никъде, освен като ред в списъка „Отложени".
-- Преди това виж колко са:
--   select count(*) from public.task_completions
--    where status='postponed' and postponed_to is not null;
--
-- DROP COLUMN маха и двете проверки, и частичния индекс — те зависят от
-- нея. Останалите ограничения на таблицата не се пипат.

alter table public.task_completions
  drop column if exists postponed_to;
