-- Rollback на 20260911204550_recurring_task_periods.sql
--
-- ⚠️ Първо се връща КЛИЕНТЪТ (и едж функциите, ако вече четат таблицата).
-- Изчезне ли таблицата, докато кодът я чете, заявката връща PGRST205 —
-- sbGet показва червен toast и връща [], тоест бюлетинът няма да покаже
-- нито една постоянна задача за нито една седмица.
--
-- ⚠️ Трие всички периоди, вкл. натисканията на „Спри"/„Активирай" след
-- прилагането, и те НЕ се възстановяват. recurring_tasks.active остава както
-- е в момента — кешът е последното състояние, тоест поведението се връща
-- към „обща листа" със същите активни/спрени задачи.
-- Преди това виж:   select count(*) from public.recurring_task_periods;
--
-- DROP TABLE маха политиките, индексите и FK-то. recurring_tasks не се пипа.

drop table if exists public.recurring_task_periods;
