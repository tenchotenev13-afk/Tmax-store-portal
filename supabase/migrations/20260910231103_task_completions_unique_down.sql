-- Rollback на 20260910231103_task_completions_unique.sql
--
-- ⚠️ ИЗТРИТИТЕ РЕДОВЕ НЕ СЕ ВЪЗСТАНОВЯВАТ.
-- Този rollback маха САМО двата индекса. Деветнайсетте излишни реда от
-- датираните групи ги няма и няма откъде да се върнат: миграцията не пази
-- копие, а PostgreSQL не помни изтритото. Ако това е неприемливо, върни ги
-- от бекъп/огледалото на Живко ПРЕДИ да разчиташ на този файл.
--
-- Какво СЕ връща: възможността един и същи обект да има повече от една
-- отметка за една и съща задача в един и същи ден. Тоест точно състоянието,
-- което дефектът в клиента произвеждаше — и ако клиентската поправка
-- (POST → при 409 PATCH) още е на живо, тя просто няма да получава 409 и ще
-- POST-ва при всеки стар bulComps, както преди.

drop index if exists public.task_completions_uniq_task;
drop index if exists public.task_completions_uniq_recurring;
