-- ═══ ПОКРИТА ОТ МИГРАЦИЯ ═══════════════════════════════════════════════
-- Записът за схемата е supabase/migrations/20260819125223_add_attachments_to_bulletin_tasks.sql
-- Този файл е историческа бележка: не го пускай и не го приемай за
-- източник на истината. Сверено на 19.08.2026.
-- Миграцията описва ЗАВАРЕНО състояние — колоната е жива в базата отпреди
-- папката с миграции и нищо не е прилагано в базата, за да се появи.
-- ══════════════════════════════════════════════════════════════════════

-- Добавя поле за снимки/файлове към задачите в бюлетина (bulletin_tasks)
-- Формат: масив от {type:'image'|'file', url, filename}
alter table bulletin_tasks add column if not exists attachments jsonb default '[]'::jsonb;
